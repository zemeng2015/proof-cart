# No .env file, store, or credentials are required for the fixture demo.
# Pass PROOF_CART_MODE=fixture in the process environment if setting a mode.
# Other modes fail closed until a provider is implemented and authorized.
